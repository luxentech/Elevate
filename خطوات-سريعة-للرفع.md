# 🚀 خطوات سريعة لرفع الموقع على Vercel

## ✅ الخطوة 1: تجهيز Supabase (5 دقائق)

1. افتح: https://supabase.com/dashboard
2. اختار المشروع: `qdxwpohacuzxvktcpxvi`
3. من القائمة الجانبية → **SQL Editor**
4. افتح ملف `supabase-setup.sql` من المشروع
5. انسخ كل محتواه والصقه في المحرر
6. اضغط **RUN** (أو Ctrl+Enter)
7. انتظر رسالة: ✅ **Success**

---

## ✅ الخطوة 2: رفع على GitHub (3 دقائق)

افتح Terminal/CMD في مجلد المشروع:

```bash
# الانتقال للمجلد
cd "C:\Users\OMAR AYMAN\Desktop\public-partner-portal-main"

# تهيئة Git (إذا لم يكن متهيأً)
git init

# إضافة كل الملفات
git add .

# عمل Commit
git commit -m "Production ready with Supabase sync"

# ربط بـ GitHub (غيّر YOUR-USERNAME)
git remote add origin https://github.com/YOUR-USERNAME/elevate-portal.git

# رفع الكود
git push -u origin main
```

**ملاحظة:** إذا ظهرت رسالة خطأ، جرب:
```bash
git push -u origin main --force
```

---

## ✅ الخطوة 3: الرفع على Vercel (5 دقائق)

### A. إنشاء حساب Vercel (إذا لم يكن لديك)
1. افتح: https://vercel.com/signup
2. سجل باستخدام GitHub

### B. رفع المشروع
1. افتح: https://vercel.com/new
2. اختار **Import Git Repository**
3. اختار `elevate-portal` (أو اسم الريبو اللي عملته)
4. **مهم جداً!** قبل الضغط على Deploy:

### C. إضافة Environment Variables
اضغط على **Environment Variables** وأضف:

| Name | Value |
|------|-------|
| `SUPABASE_URL` | `https://qdxwpohacuzxvktcpxvi.supabase.co` |
| `SUPABASE_PUBLISHABLE_KEY` | `sb_publishable_0Q_RXWs0cfen9EooZhkvTw_Pe-OycaN` |
| `SUPABASE_PROJECT_ID` | `qdxwpohacuzxvktcpxvi` |
| `VITE_SUPABASE_URL` | `https://qdxwpohacuzxvktcpxvi.supabase.co` |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | `sb_publishable_0Q_RXWs0cfen9EooZhkvTw_Pe-OycaN` |
| `VITE_SUPABASE_PROJECT_ID` | `qdxwpohacuzxvktcpxvi` |

5. اضغط **Deploy**
6. انتظر 2-3 دقائق

---

## ✅ الخطوة 4: اختبار الموقع (دقيقتان)

1. بعد اكتمال الـ Deploy، اضغط **Visit**
2. افتح الموقع
3. سجل دخول كـ Admin
4. اذهب لـ **لوحة التحكم**
5. أضف **شريك اختبار** (مثلاً: "Google")
6. **افتح الموقع في Private Window**
7. يجب أن ترى الشريك! ✅

---

## 🎉 تم بنجاح!

موقعك الآن:
- ✅ Live على Vercel
- ✅ متصل بـ Supabase
- ✅ البيانات تظهر لجميع المستخدمين
- ✅ Sync تلقائي كل 15 ثانية

### الرابط الخاص بك:
```
https://your-project-name.vercel.app
```

---

## 🆘 مشاكل شائعة

### ❌ "Missing environment variables"
**الحل:** تأكد من إضافة كل الـ 6 متغيرات في Vercel

### ❌ البيانات لا تظهر
**الحل:** 
1. افتح Console (F12)
2. ابحث عن أخطاء
3. تأكد من تنفيذ `supabase-setup.sql`

### ❌ خطأ 500 من API
**الحل:**
1. Vercel Dashboard → Functions → Logs
2. شاهد الخطأ
3. غالباً: Environment Variables خاطئة

---

## 📞 تحتاج مساعدة؟

- واتساب: **+201124310907**
- بريد: **contact.luxentech@gmail.com**

---

## 🔄 التحديثات المستقبلية

عشان تحدث الموقع مستقبلاً:

```bash
# عمل تعديلات على الكود
git add .
git commit -m "وصف التحديث"
git push origin main
```

**Vercel هيعمل Deploy تلقائياً!** ✨

---

بالتوفيق! 🚀
